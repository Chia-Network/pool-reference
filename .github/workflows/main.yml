name: black-action
on: 
  push:
    branches: main
  pull_request:
jobs:
  linter_name:
    name: runner & black formatter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: psf/black@stable
        with:
          black_args: ". --check"
          src: "."
